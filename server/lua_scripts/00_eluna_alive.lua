-- Proof-of-life only. If this line is not in the server console / info.log at startup,
-- Eluna is not running and nothing else in lua_scripts/ is either.
-- Safe to delete once Eluna is confirmed.
print("[Eluna] ALIVE - Lua engine loaded, scripts are running")
